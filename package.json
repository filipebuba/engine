{
  "name": "edital-match-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Motor soberano do EDITAL MATCH — matching de editais com IA 100% local (qwen/Ollama)",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "build": "node node_modules/typescript/bin/tsc -p tsconfig.build.json",
    "verify": "bash harness/verify.sh",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
